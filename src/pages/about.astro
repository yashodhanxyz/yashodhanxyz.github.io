---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Image } from 'astro:assets';
import aboutJourney from '../assets/site/about-journey.svg';
import { createPersonJsonLd, createWebsiteJsonLd } from '../utils/site';

const timeline = [
  {
    company: 'Order',
    role: 'Founding Product Engineer',
    period: '2023 — Present',
    summary:
      'Standing up the workflow engine, design system, and feedback loops that bring fulfillment teams into one calm control center.',
    slug: 'order',
  },
  {
    company: 'The Legal Chronicles',
    role: 'Product & Storytelling Lead',
    period: '2022 — 2023',
    summary:
      'Shaped the editorial pipeline and interactive reading experience so legal insights felt human, not heavy.',
    slug: 'legal-chronicles',
  },
  {
    company: 'Studio Sprints',
    role: 'Fractional Product Partner',
    period: '2019 — 2022',
    summary:
      'Guided early-stage teams through discovery, prototyping, and the first production launch across multiple industries.',
    slug: 'studio-sprints',
  },
];

const structuredData = [createPersonJsonLd(), createWebsiteJsonLd()];
---

<BaseLayout
  title="About Yashodhan"
  description="Tracing the story behind Order, trusted partnerships, and the craft that powers my work."
  structuredData={structuredData}
>
  <section class="about-intro" aria-labelledby="about-heading">
    <p class="eyebrow">About</p>
    <h1 id="about-heading">Every product win begins with curiosity and care.</h1>
    <p>
      I grew up reverse-engineering interfaces, sketching how information could flow faster, clearer, and with more heart. That
      obsession turned into a practice: helping product-led teams navigate ambiguity, align on the right bets, and ship
      experiences people can rely on.
    </p>
  </section>

  <section class="about-photo" aria-label="Collage illustrating my journey">
    <Image
      src={aboutJourney}
      alt="Abstract illustration showing milestones across Yashodhan's product journey"
      widths={[600, 896]}
      sizes="(max-width: 900px) 100vw, 896px"
      loading="lazy"
    />
  </section>

  <section class="about-story" aria-labelledby="story-heading">
    <h2 id="story-heading">Building Order with steady confidence</h2>
    <p>
      Order started as a messy wall of whiteboard sketches—operations leaders frustrated by delays, customer support teams
      triaging from spreadsheets, and executives hungry for signal without the noise. I joined as the founding product engineer
      to translate that energy into a dependable orchestration platform.
    </p>
    <p>
      My first three months were all about framing the problem: shadowing fulfillment teams, mapping every edge case, and teasing
      apart when humans should stay in the loop. From there, I built the workflow engine that powers our automation, authored the
      shared language in our design system, and established an internal feedback cadence so insights flowed back into the product
      within 48 hours.
    </p>
    <p>
      We now route thousands of shipments each week through Order with calm dashboards, guardrails for risky scenarios, and a
      roadmap that stays honest because the people doing the work can see their fingerprints on every release.
    </p>
  </section>

  <section class="timeline" aria-labelledby="timeline-heading">
    <div class="timeline__header">
      <h2 id="timeline-heading">Recent Experience</h2>
      <p>Follow the threads that shaped how I build.</p>
    </div>
    <ol>
      {timeline.map((item) => (
        <li>
          <a class="timeline__card" href={`/work/${item.slug}`}>
            <span class="timeline__role">{item.role}</span>
            <span class="timeline__company">{item.company}</span>
            <span class="timeline__period">{item.period}</span>
            <p class="timeline__summary">{item.summary}</p>
            <span class="timeline__cta" aria-hidden="true">Read the story →</span>
          </a>
        </li>
      ))}
    </ol>
  </section>

  <section class="resume" aria-labelledby="resume-heading">
    <div class="resume__content">
      <h2 id="resume-heading">Keep exploring</h2>
      <p>
        Snapshot the highlights or dive into the full story. Grab the PDF resume, or browse a web version that links out to
        projects and writing.
      </p>
      <div class="resume__links">
        <a class="resume__button" href="/resume.pdf" data-analytics-goal="Resume download">Download Resume</a>
        <a class="resume__link" href="/resume">View web resume</a>
      </div>
    </div>
  </section>

  <style>
    .about-intro {
      max-width: 70ch;
      display: grid;
      gap: 1rem;
      margin-bottom: 3.5rem;
    }

    .about-intro h1 {
      margin: 0;
      font-size: clamp(2rem, 4vw, 2.6rem);
      line-height: 1.2;
    }

    .eyebrow {
      font-size: 0.85rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
      color: var(--color-muted);
    }

    .about-photo {
      margin: 0 0 4rem;
      border-radius: var(--radius-base);
      overflow: hidden;
      border: 1px solid var(--color-border);
      background: var(--color-surface);
    }

    .about-photo :global(img) {
      width: 100%;
      height: auto;
      display: block;
    }

    .about-story {
      max-width: 72ch;
      display: grid;
      gap: 1.2rem;
      margin-bottom: 4rem;
    }

    .about-story h2 {
      font-size: clamp(1.8rem, 3vw, 2.2rem);
      margin: 0;
    }

    .timeline {
      border-top: 1px solid var(--color-border);
      padding-top: 3rem;
      margin-top: 2rem;
      margin-bottom: 4rem;
    }

    .timeline__header {
      display: flex;
      flex-direction: column;
      gap: 0.5rem;
      margin-bottom: 2rem;
    }

    .timeline__header h2 {
      margin: 0;
    }

    .timeline ol {
      list-style: none;
      padding: 0;
      margin: 0;
      display: grid;
      gap: 1rem;
    }

    .timeline__card {
      display: grid;
      gap: 0.35rem;
      padding: 1.75rem;
      border-radius: var(--radius-base);
      border: 1px solid var(--color-border);
      background: var(--color-surface);
      transition: transform var(--transition-speed) ease, box-shadow var(--transition-speed) ease;
      color: inherit;
    }

    .timeline__card:focus-visible {
      outline: 2px solid var(--color-accent);
      outline-offset: 3px;
    }

    .timeline__card:hover,
    .timeline__card:focus-visible {
      transform: translateY(-2px);
      box-shadow: 0 18px 36px rgba(15, 23, 42, 0.08);
    }

    .timeline__role {
      font-size: 1.1rem;
      font-weight: 600;
    }

    .timeline__company {
      color: var(--color-muted);
    }

    .timeline__period {
      font-size: 0.9rem;
      color: var(--color-muted);
      letter-spacing: 0.02em;
    }

    .timeline__summary {
      margin: 0.5rem 0 0;
      line-height: 1.65;
      color: inherit;
    }

    .timeline__cta {
      font-weight: 600;
      color: var(--color-accent);
      margin-top: 0.5rem;
    }

    .resume {
      border-top: 1px solid var(--color-border);
      padding-top: 3rem;
      margin-top: 2rem;
      margin-bottom: 4rem;
    }

    .resume__content {
      max-width: 64ch;
      display: grid;
      gap: 1rem;
    }

    .resume__links {
      display: inline-flex;
      flex-wrap: wrap;
      gap: 1rem;
      align-items: center;
    }

    .resume__button {
      display: inline-flex;
      align-items: center;
      justify-content: center;
      padding: 0.75rem 1.4rem;
      border-radius: 999px;
      background: var(--color-accent);
      color: #f8fafc;
      font-weight: 600;
      border: none;
    }

    .resume__button:hover,
    .resume__button:focus-visible {
      text-decoration: none;
      box-shadow: 0 16px 30px rgba(37, 99, 235, 0.25);
    }

    .resume__button:focus-visible {
      outline: 2px solid #f8fafc;
      outline-offset: 3px;
    }

    .resume__link {
      font-weight: 600;
      color: var(--color-accent);
    }

    .resume__link:hover,
    .resume__link:focus-visible {
      text-decoration: underline;
    }

    @media (max-width: 720px) {
      .timeline__card {
        padding: 1.4rem;
      }

      .resume__links {
        flex-direction: column;
        align-items: flex-start;
      }
    }
  </style>
</BaseLayout>
